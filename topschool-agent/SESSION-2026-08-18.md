# Session Summary — 2026-08-18

## What was done

### Task #32: Agent 對話功能接入
- 重寫 `apps/web/app/agent/page.tsx` 為多輪聊天介面
- 更新 `/api/v1/agent/query/route.ts` 支援 `messages[]` 參數
- 加入 `buildContextQuery` 函式處理對話歷史
- 加入範例查詢、清除對話、loading 動畫

### Agent 角色更新
- 將 Agent 角色從「資料庫查詢翻譯器」升級為「香港 K12 個性化教育與升學規劃師」
- 更新 `apps/web/lib/agent-query.ts` 的 system prompt
- 加入教育理念、決策原則、禁止行為等專業框架

### 問題發現
- **LongCat-2.0 中文結構化輸出問題**：
  - 英文查詢 `secondary school Kowloon` → 成功輸出 `{"level":"secondary","district":"九龍"}`
  - 中文查詢 `中學 九龍城區` → 輸出空 filter `{"limit":20}`
  - 這是 LongCat 模型限制，非程式碼錯誤

### TypeScript 修復
- 修復 `apps/web/app/api/v1/schools/favourites/route.ts` 的 `SQL` 型別錯誤
- 修復 `apps/web/app/search/page.tsx` 的 `metadata` 型別錯誤
- 更新 `tsconfig.json` 排除臨時腳本 `check_*.ts`

## Files Modified

| File | Change |
|------|--------|
| `apps/web/app/agent/page.tsx` | 多輪聊天介面 |
| `apps/web/app/api/v1/agent/query/route.ts` | 支援 messages[] |
| `apps/web/lib/agent-query.ts` | K12 升學規劃師 prompt |
| `apps/web/app/api/v1/schools/favourites/route.ts` | TS 修復 |
| `apps/web/app/search/page.tsx` | TS 修復 |
| `apps/web/tsconfig.json` | 排除臨時腳本 |

## TODO

### 高優先級
1. **Agent 中文查詢修復**：
   - 方案 A：換用 Claude API（對中文 JSON 輸出更穩定）
   - 方案 B：在 prompt 加入「先翻譯成英文」指示
   - 方案 C：用 Few-shot examples 改善輸出

2. **資料庫補全**：
   - 「一條龍」「直屬」「聯繫」學校關聯
   - 學費、DSE 表現、IB 表現

### 中優先級
3. 圖片支援
4. 用戶系統
5. 進階篩選

## System Status

- ✅ Dev server: http://localhost:3006
- ✅ Database: PostgreSQL connected (~2,200 schools)
- ⚠️ Agent: 英文查詢正常，中文待修復
- ✅ TypeScript: 0 errors
